/*
 * A (functional) interface where the method takes no arguments
 * and returns a Parent node.
 */
package Calendar_Roberto_Aguinaga;

import javafx.scene.Parent;

public interface ReturnContent {
    Parent getContent();
}
